'use strict';

/**
 * product-verification router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::product-verification.product-verification');
